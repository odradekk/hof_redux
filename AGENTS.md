# HOF Redux

## 目标与范围

- 本项目是以 `old_hof/` 为玩法蓝本的完整重写，托管仓库为 `https://github.com/odradekk/hof_redux`。
- 首期覆盖旧版全部正式玩家功能和管理能力；废弃、实验和重复实现不逐个复刻。先完成并稳定旧版重写，再增加新系统。
- 优先保留旧版职业、物品、技能、公式和玩法。对明确错误记录证据与修复后的预期；代码、手册或用户描述有歧义时，先形成待决问题，避免静默改动规则。
- 新版不承担旧代码、接口、存档或数据格式兼容性。`old_hof/` 是只读参考；提取游戏内容与素材时保留来源，不把旧实现搬入生产代码。
- 复用旧素材，全面重新设计界面。前端以文字、图标、卡片和战报为主，移动端优先，兼顾桌面端；首期不增加战斗动画制作范围。
- 账号采用账号密码登录，暂不接入邮件服务，取消闲置自动删号。保留城镇留言和可配置的外部论坛链接，不额外开发完整论坛。
- 用户负责方向、监督和验收，AI 负责主要工程工作。使用中文沟通；方案和报告区分已确认决定、代码事实、推测与待验证项。

## 技术基线

- 前端：Vue 3、Vite、TypeScript。后端：Node.js、Fastify、TypeScript。
- 采用单仓库、模块化单体后端；前端和后端可分别构建，生产首期部署在一台服务器。
- 数据库采用 SQLite，启用 WAL，文件位于本机持久磁盘。约 100 人在线是首轮负载测试目标，不是已经验证的承载能力。
- 游戏内容采用版本化 JSON 定义，经校验后随应用版本发布。运行期玩家状态保存在数据库。
- 依赖的具体版本、驱动和构建细节在相应设计决定中确定，不把未验证的库或命令写成项目现状。

## 模块职责

| 模块 | 负责的内容 |
| --- | --- |
| 账号 | 注册、登录、会话和账号管理 |
| 角色与编队 | 成长、职业、转职、技能学习、战术条件和阵型 |
| 物品与资产 | 背包、装备、货币、体力、制作、精炼和装备效果 |
| 战斗引擎 | 行动顺序、条件判断、技能效果、状态变化、胜负与结构化战斗记录 |
| 地图冒险 | 地图开放、遭遇生成、普通战斗与模拟战流程、奖励结算 |
| 竞技场 | 排名、挑战、防守队伍和冷却 |
| 世界 Boss | 全服共享状态、挑战限制、复活和奖励结算 |
| 交易 | 商店、拍卖、竞价与到期结算 |
| 社区与运营 | 留言、公告、图鉴、手册、战报查询和全部正式管理工具 |

- 这些是职责约定，不要求每个模块都成为独立软件包或服务。
- 旧版 `Union` 指全服共享世界 Boss，不是公会。核心战斗是战前配置条件式战术、提交后按速度与行动延迟自动演算；保留五人编队与前后排等已确认玩法。
- 战斗引擎接收状态与规则输入，返回结果和记录，不访问数据库、HTTP 或页面。同一规则输入与随机输入下应能重复验证；版本标识与记录格式在详细设计中确定。
- 各模块管理自己的状态，通过少量明确的操作协作。跨模块读取使用明确的数据接口；跨模块变更通过所属模块操作完成。
- 购买、竞价、奖励等一次业务操作的相关资产和状态变更在同一事务中提交。模块不能各自提前提交，失败不能留下部分结算。
- 战斗计算与写事务保持分离；提交时验证依赖状态仍有效。世界 Boss、拍卖和重复请求需要专门的并发与幂等设计，不能直接覆盖旧快照。
- 界面、HTTP 入口和存储代码分别调用游戏模块，避免在页面或请求处理器中散落游戏规则。

## 内容与扩展

- 职业、物品、怪物、技能、地图、掉落和配方使用 JSON 定义，以稳定标识相互引用，展示名称不充当身份标识。
- 发布前校验字段、重复标识、缺失引用及机制约束。职业通过转职关系连接，代码不写死最高转职次数。
- 通用技能由已有效果组合表达；特殊机制引用具名代码处理器。配置中不执行任意脚本。
- 内容定义与玩家持有状态分开；精炼和附加效果使用明确字段，不编码进物品编号。
- 数据扩展：使用已有机制的新内容，通过配置和素材完成。纵向扩展：新增机制的代码集中在相关模块。横向扩展：新系统通过已有模块的公开操作协作。
- 用旧版已有精炼、装备效果和技能机制检验扩展能力。镶嵌、连战副本、任务、家园及三转新内容是未来扩展示例，不属于首期功能。
- 每个抽象解决当前具体问题。优先复用实现、标准库和已有依赖；避免为尚不存在的玩法建立通用插件平台。

## 实施与验收

- 先建立“旧功能与规则 → 新模块 → 验收场景”对应清单，明确正式入口、备用实现、已知错误与批准的行为差异。
- 分阶段交付可玩版本：登录/编队/普通战斗/保存/手机战报；成长/战术/装备/地图；交易/竞技/世界 Boss；其余正式功能；整体运行验证。
- 分阶段是实施顺序，完成标准仍是全部正式系统覆盖。不得用核心循环可玩代替整体交付。
- 验证内容完整性、关键规则、事务回滚、重复请求和并发结算；用真实操作组合验证负载，记录硬件、操作频率、延迟和错误率。
- 验证手机端主要流程以及数据库备份恢复。SQLite 使用一致性备份方式，不能把运行中的主数据库文件单独复制视为完整备份。
- 测试断言可观察行为和契约；优先确定性的场景，避免以实现细节或当前样例反向定义规则。

## 工作约定

- 阅读与规划请求只做调查和规划；实现按已批准范围和当前任务推进。Wayfinder 地图是决策规划，开发交付另行组织。
- 保留陌生文件和未提交改动。提交、推送、合并和部署遵循用户明确请求或已建立的流程；保持已发布历史。
- 旧目录可能包含账号样本、凭据及运行日志；提取和发布资料时只使用任务所需的代码、内容定义和素材，不发布这些运行数据或凭据。
- 重型探索、独立核验和边界明确的支线可交给子代理，主代理保留核心设计与实施决策，并核查子代理报告。
- 子代理显式指定模型：高难度任务和所有审查使用 `gpt-5.6-sol`；简单快速任务使用 `gpt-5.6-luna`；其他常规任务使用 `gpt-5.6-terra`。任务说明应包含目标、范围、已知证据和交付物。
- 文档仅记录已确认事实和约定；使用已有文档检查，文档变更不新增无关测试。交付说明结果、验证方式和剩余工作。

### 旧版运行参考

- `old_hof/` 当前通过本机 Docker 运行，部署配置为 `docker-compose.legacy.yml`，服务地址为 `http://127.0.0.1:60000`。
- 需要确认或验证旧版的可观察行为时，可向该地址发起 HTTP 请求；该实例仅作为玩法和行为参考，仍须遵守旧目录只读及敏感数据边界。

### 新版工程结构（S1 基线）

- pnpm 单仓库：`apps/frontend`（Vue 3 + Vite）、`apps/backend`（Fastify + `node:sqlite`）、`packages/shared`（共享接口契约类型）、`content/`（版本化内容发布清单）、`apps/backend/migrations/`（带版本 SQL 迁移）。
- 四个隔离实例由 `compose/compose.{dev,test,trial,recovery}.yml` 定义，入口端口 61000/62000/63000/64000（仅回环），数据分别在 `var/<实例>/db`（不入库）；旧版参照栈 `hof-legacy` 占用 60000，不得冲突或复用。
- 自动检查在 `scripts/`：`check-build.sh`、`check-health.sh`、`check-restart-persistence.sh`，整体验证 `verify-baseline.sh`；用法与锁定版本见 [docs/run/engineering-baseline.md](docs/run/engineering-baseline.md)。

## Agent skills

### Issue tracker

需求、规格和 Wayfinder 决策使用 `odradekk/hof_redux` 的 GitHub Issues。操作前阅读 [docs/agents/issue-tracker.md](docs/agents/issue-tracker.md)。

### Domain docs

采用 single-context。探索、设计或更改领域术语与架构决定前，阅读 [docs/agents/domain.md](docs/agents/domain.md)，并按其规则加载领域文档。
